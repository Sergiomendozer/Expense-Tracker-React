import React from "react";

// Header component for the Expense Tracker app
export const Header = () => {
  return (
    <h2>Expense Tracker {/* Text displayed as the main title of the app */}</h2>
  );
};
